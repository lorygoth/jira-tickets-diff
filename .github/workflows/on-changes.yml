name: Test & Build

on:
  push:
    branches: ['!main']
  pull_request:
    branches: ['main']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/common/build.yml
        with:
          environment: test
      - uses: ./.github/workflows/common/test.yml
